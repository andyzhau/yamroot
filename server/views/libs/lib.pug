mixin lib(tracking, domain)
  script
    include lib.js
  script.
    if (rt.tracking._id == null) {
      rt.tracking = JSON.parse('!{JSON.stringify(tracking)}');
      rt.domain = '#{domain}';
    }
